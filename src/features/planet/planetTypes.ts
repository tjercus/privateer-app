type coordinateLat = number;
type coordinateLong = number;

export type PlanetFormDataMap = Map<
  "coordinateLat" | "coordinateLong" | "id" | "name",
  any
>;
export type PlanetHtmlInputType = "number" | "text";
