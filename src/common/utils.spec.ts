
import { describe, expect } from "vitest";

describe("utils", () => {
  describe("hasIssues", () => {
    const validationIssues = [{path: ["yoda"]}, {path: ["vader"]}];
    it("should ", () => {});
  });
});